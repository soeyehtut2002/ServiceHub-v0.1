{
  "name": "servicehub",
  "version": "1.0.0",
  "description": "ServiceHub monorepo root",
  "scripts": {
    "setup-db": "node setup-db.js",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "dotenv": "^16.4.5"
  }
}
